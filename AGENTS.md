# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Aly-El-Bachir-DAM`) is a multi-purpose sandbox repository. The `main` branch contains only a `README.md` — there is no application code, no dependencies, and no services to run.

- **No build/lint/test commands exist** on the `main` branch. There is nothing to install or start.
- Various remote feature branches contain unrelated projects (websites, diagnostic tools, etc.), but none are merged into `main`.
- If future work adds application code, update this file with relevant build, lint, test, and run instructions.
